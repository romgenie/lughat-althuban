<div dir="rtl">

# نظرة عامة على مشروع apython

**apython** لهجة برمجية عربية مبنية فوق بايثون. تكتب الكلمات المفتاحية والدوال المدمجة والاستثناءات بالعربية، وتُترجم الملفات إلى بايثون القياسي في وقت التحميل وتُنفَّذ بواسطة CPython — دون تشعيب للمترجم أو تعديل على البنية التركيبية للغة.

---

## ما هو apython؟

apython هو **معالج مسبق قائم على الرموز المعجمية** (tokenizer-based preprocessor). عندما تشغّل ملف `.apy` يمر بالمراحل التالية:

```
المدخل العربي (.apy)
        ↓
pretokenize  ← تحويل الأرقام العربية، استبدال علامات الترقيم، رفض محارف bidi المحظورة
        ↓
tokenize     ← مُرمِّز بايثون القياسي
        ↓
translate    ← إعادة كتابة أسماء المعرّفات العربية إلى نظيراتها الإنجليزية
        ↓
untokenize + compile + exec
        ↓
تنفيذ CPython عادي
```

لا يوجد تعديل على شجرة AST ولا تشعيب لـ CPython. المشروع مُصمَّم معمارياً على غرار مشروع [zhpy](https://github.com/zhpy/zhpy) للصينية.

---

## مثال سريع

```python
# مرحبا.apy
دالة مرحبا(اسم):
    اطبع(f"مرحبا يا {اسم}")

لكل شخص في ["سارة", "أحمد", "ليلى"]:
    مرحبا(شخص)
```

```bash
$ apython مرحبا.apy
مرحبا يا سارة
مرحبا يا أحمد
مرحبا يا ليلى
```

---

## التثبيت

يتطلب بايثون 3.11 أو أحدث.

```bash
git clone https://github.com/GalaxyRuler/apython
cd apython
pip install -e .
```

يُثبِّت الأمر نصّ الدخول `apython` في بيئتك.

---

## طريقة التشغيل

أربع واجهات دخول تعكس واجهة `python` القياسية:

| الأمر | الوصف |
|-------|--------|
| `apython ملف.apy` | تشغيل ملف |
| `apython -c 'اطبع("مرحبا")'` | تنفيذ سلسلة نصية مباشرة |
| `apython - < ملف.apy` | القراءة من المدخل القياسي |
| `apython` | فتح بيئة تفاعلية REPL |

```bash
apython --help     # عرض التعليمات
apython --version  # عرض رقم الإصدار
```

رموز الخروج: `0` نجاح، `1` خطأ تشغيل أو ترجمة، `2` خطأ استخدام.

---

## استيراد ملفات `.apy`

يُثبِّت apython خطّاف استيراد في `sys.meta_path` يتيح استيراد ملفات `.apy` كأي وحدة بايثون عادية:

```python
# main.apy
استورد helper كـ مساعد
اطبع(مساعد.مربع(5))
```

```python
# helper.apy
دالة مربع(س):
    ارجع س ** 2
```

```bash
$ apython main.apy
25
```

تعمل الحزم المختلطة (`.py` + `.apy`) بشكل طبيعي. يرى ملف `.py` الذي يستورد وحدة `.apy` الأسماء بصيغتها المُطبَّعة (المُنوَّرمة) وفق ADR 0004.

---

## رسائل الخطأ بالعربية

تصدر رسائل الخطأ غير المُعالَجة بالعربية تلقائياً:

```bash
$ apython -c '1 / 0'
تتبع_الأخطاء (المكدس الأحدث آخراً):
  ملف "<string>", سطر 1, في <الوحدة>
خطأ_قسمة_صفر: القسمة على صفر
```

يترجم apython 38 نوعاً من الاستثناءات القياسية ونحو 30 رسالة شائعة من رسائل المترجم. الأنواع والرسائل غير المُعرَّفة تمر دون تغيير.

---

## قاموس الكلمات المفتاحية

القاموس الكامل للكلمات المفتاحية والدوال المدمجة والاستثناءات موجود في:

| الملف | الوصف |
|-------|--------|
| [`dictionaries/ar-v1.md`](../../dictionaries/ar-v1.md) | 187 مدخلة: 145 اسماً + 42 خاصية (الإصدار 1.1) |
| [`dictionaries/exceptions-ar-v1.md`](../../dictionaries/exceptions-ar-v1.md) | 38 استثناءً + تسلسل هرمي للاستثناءات وترجمات رسائل المترجم |

الإصدار `ar-v1` مجمَّد وفق [ADR 0008](../../decisions/0008-phase-b-charter.md) §B.0. أي تغيير على الكلمات القائمة يتطلب إصدار `ar-v2` + قرار ADR جديد.

**عينة من الكلمات المفتاحية:**

| بايثون | عربي | بايثون | عربي |
|--------|-------|--------|-------|
| `if` | `إذا` | `for` | `لكل` |
| `def` | `دالة` | `class` | `صنف` |
| `return` | `ارجع` | `import` | `استورد` |
| `print` | `اطبع` | `True` | `صحيح` |
| `False` | `خطأ` | `None` | `عدم` |

للاطلاع على القائمة الكاملة راجع [`dictionaries/ar-v1.md`](../../dictionaries/ar-v1.md).

---

## هيكل المشروع

```
apython/
├── arabicpython/          الحزمة الرئيسية للمعالج
│   ├── pretokenize.py     المرحلة الأولى: الأرقام والترقيم وبيدي
│   ├── normalize.py       طيّ حروف الهمزة والتاء المربوطة والحركات
│   ├── dialect.py         تحميل القاموس وتجميد الحالة
│   ├── translate.py       إعادة كتابة الرموز المعجمية
│   ├── importer.py        خطّاف الاستيراد sys.meta_path
│   ├── repl.py            بيئة REPL التفاعلية
│   ├── excepthook.py      ترجمة رسائل الخطأ
│   └── cli.py             واجهة سطر الأوامر
├── decisions/             قرارات التصميم (ADRs) — 9 قرارات
├── dictionaries/          ar-v1.md + exceptions-ar-v1.md
├── docs/
│   ├── getting-started.md دليل البدء (بالإنجليزية)
│   └── ar/                التوثيق بالعربية ← أنت هنا
├── examples/              7 أمثلة تعليمية تصاعدية
├── specs/                 حزم المواصفات (0001–0011)
└── tests/                 مجموعة pytest (351 اختباراً، 21 مُتجاوَزاً)
```

---

## المعمارية في فقرة واحدة

يمر المدخل عبر `pretokenize` (طيّ الأرقام العربية-الهندية إلى ASCII، استبدال علامات الترقيم، رفض 12 محرفاً من محارف bidi وفق UAX #9) ← `tokenize` الخاصة ببايثون ← مُعيد كتابة الأسماء الذي يستشير قاموس عربي→بايثون المجمَّد ← `untokenize` ← `compile` ← `exec`. لا تعديل على شجرة AST، ولا تشعيب لـ CPython. المسار ذاته يدعم جميع واجهات الدخول (CLI، REPL، خطّاف الاستيراد).

---

## خارطة الطريق

| المرحلة | المحتوى | الحالة |
|---------|---------|--------|
| 0 | قرارات التصميم (9 ADRs) | مكتملة |
| A | المعالج التوكينزي: pretokenize، normalize، translate، CLI، خطّاف الاستيراد، REPL، رسائل الخطأ بالعربية | **مكتملة** |
| B | بديل الإنتاج — نطاق المرحلة وشروط الدخول محددة في [ADR 0008](../../decisions/0008-phase-b-charter.md) | لم تبدأ بعد |

---

## الحدود المعروفة (المرحلة A)

- **`from . import x` داخل `__init__.apy`** لا تُترجم — الحلّ: `import pkg.sub as sub`.
- **الوصول إلى الخصائص من `.py` إلى `.apy`** يتطلب الشكل المُطبَّع (المُنوَّرم) وفق [ADR 0004](../../decisions/0004-normalization-policy.md).
- **لا اختبارات شاملة للبرمجة غير المتزامنة** — الكلمات المفتاحية تُترجم، لكن البرامج الفعلية لـ `async/await` ليست مُختبَرة في هذه المرحلة.

---

## الشكر والاعتراف

- **zhpy** (gasolin، 2014) — لهجة بايثون الصينية التي يعتمد عليها هذا المشروع معمارياً.
- **قلب (Qalb)** لرمزي ناصر — النقد الذي يأخذه هذا المشروع بجدية، وسبب جعل المرحلة B مرحلة مستقلة مؤجلة بدلاً من كونها الهدف الافتراضي.
- **Hedy** — دليل على أن بايثون التعليمية متعددة اللغات والتدريجية تعمل على نطاق واسع.

</div>
