# Minimal valid mapping — used by test_loader_parses_valid_minimal
# References `sys` (always available) with two entries.

[meta]
arabic_name   = "نظام"
python_module = "sys"
dict_version  = "ar-v1"
schema_version = 1
maintainer    = "—"

[entries]
"وسائط" = "argv"
"مسارات" = "path"
